# The Friendship Practice - replacement visual assets

Upload these files to replace the current visual assets.

Recommended:
- Replace the entire `icons/` folder with the `icons/` folder in this zip.
- Replace the root `og-image.jpg` with the one in this zip.

This zip also includes root-level favicon files for compatibility with older pages that still reference `/favicon.svg` or `/favicon.ico`.
